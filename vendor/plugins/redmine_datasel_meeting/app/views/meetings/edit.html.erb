<h2><%=l(:label_edit_meeting)%></h2>
<% labelled_tabular_form_for :meeting, @meeting, :url => { :action => "create", :project_id => @project } do |f| %>
<%= render :partial => 'form', :locals => {:f => f} %>
<% end %>
