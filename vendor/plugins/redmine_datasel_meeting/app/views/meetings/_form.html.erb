<% labelled_tabular_form_for :meeting, @project, :url => { :action => "create" } do |f| %>
<p><%= f.text_field :subject, :size => 60 %></p>
<%= wikitoolbar_for 'summary' %>
<%= submit_tag l(:button_save) %>
<% end %>