<h2>Meetings#index</h2>
<table>
	<tr>
		<th>Subject</th>
	</tr>
<% @meetings.each do |meeting| %>
	<tr>
		<td><%= meeting.subject %></td>
	</tr>
<% end %>
</table>
<%= link_to 'New Meeting', { :action => 'new', :project_id => @project.identifier} %>