<div class="contextual"><%= link_to 'New Meeting', { :action => 'new', :project_id => @project.identifier},:class => 'icon icon-add' %></div>
<h2><%=l(:label_list_of_meeting)%></h2>
<div class="autoscroll">
	<table class="list">
		<tbody>
			<tr>
				<th>#</th><th>Date</th><th>Subject</th><th>Convacator</th><th>Issue</th>
			</tr>
			<% @meetings.each do |meeting| %>
			<tr>
				<td class="id"><%= link_to meeting.id, { :action => 'show', :meeting_id => meeting.id, :project_id => @project.identifier} %></td>
				<td class="updated_on"><%= format_date(meeting.date) %></td>
				<td class="subject"><%= link_to meeting.subject, { :action => 'show', :meeting_id => meeting.id, :project_id => @project.identifier} %></td>
				<td class="assigned_to"><%= meeting.convacator.name %></td>
				<td class="id"><%= link_to meeting.issue.id, :controller => 'issues', :action => 'show', :id => meeting.issue %></td>
			</tr>
			<% end %>
		</tbody>			
	</table>
</div>