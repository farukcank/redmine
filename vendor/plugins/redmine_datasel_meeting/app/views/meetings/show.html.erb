<h2><%=l(:label_meeting_details)%></h2>
<%= render :partial => 'summary', :locals => {:meeting => @meeting} %>
<tr>
	<tr><th>Agenda</th></tr>
	<td>
		<div class="wiki">
			<%= textilizable @meeting, :agenda %>
		</div>
	</td>
</tr>
<tr><th>Content</th></tr>
<tr>
	<td>
		<div class="wiki">
			<%= textilizable @meeting, :content %>
		</div>
	</td>
</tr>
</table>
<td><%= link_to 'Update', { :action => 'edit', :meeting_id => @meeting.id, :project_id => @project.identifier} %></td>